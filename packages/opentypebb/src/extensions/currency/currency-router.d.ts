/**
 * Currency Router — root router for foreign exchange market data.
 * Maps to: openbb_currency/currency_router.py
 */
import { Router } from '../../core/app/router.js';
export declare const currencyRouter: Router;
