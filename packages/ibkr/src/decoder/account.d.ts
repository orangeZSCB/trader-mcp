/**
 * Account / position decoder handlers — text + protobuf.
 *
 * Mirrors: ibapi/decoder.py (account-related process methods)
 */
import { Decoder } from './base.js';
export declare function applyAccountHandlers(decoder: Decoder): void;
