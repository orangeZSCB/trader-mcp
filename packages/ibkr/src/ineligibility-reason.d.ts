/**
 * Ineligibility reason for order preview.
 * Mirrors: ibapi/ineligibility_reason.py
 */
export declare class IneligibilityReason {
    id: string;
    description: string;
    constructor(id?: string, description?: string);
    toString(): string;
}
